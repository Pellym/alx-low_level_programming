#include "lists.h"

/**
 * listint_t -  frees a listint_t list
 * @h: head points the list
 *
 * Return void
 */

void free_listint(listint_t *head)
{
	listint_t *list;

		while (head)
		list =	head ->new;
	                 free(list);
}
