#include "lists.h"

/**
 *  frees a listint_t list
 *
 *  Return: Always 0
 *
 */

void free_listint(listint_t *head)
{
	size_t counts = 0;
		while (head != Null)
		{
			count++
		        h = h->next;
		}
	return (count);
}
